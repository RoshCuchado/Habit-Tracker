# habit-tracker
A minimal, dark-themed habit tracker that lives entirely in the browser. No backend, no accounts, no npm. Built with vanilla HTML, CSS, and JavaScript, with localStorage as the only persistence layer.
